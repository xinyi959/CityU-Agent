# OpenRouter API key (required — used for LLM routing & answer generation)
# Get one at https://openrouter.ai/keys
OPENROUTER_API_KEY="sk-or-v1-..."

# HuggingFace token (required for downloading BGE embedding model weights)
# Get one at https://huggingface.co/settings/tokens
HF_TOKEN="hf_..."
